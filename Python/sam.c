#include<stdio.h>
#include<string.h>
void main()
{
    char arr[5]={'+','h','+','k','+'};
    for(int i=0;i<strlen(arr);i++)
    {
        if(arr[i]!='h' || arr[i]!='h')
        printf("false");
        else
        printf("true");
    }
}